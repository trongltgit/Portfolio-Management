services:
  - type: web
    name: investment-ml-platform
    env: python
    plan: free

    pythonVersion: 3.11.9

    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
